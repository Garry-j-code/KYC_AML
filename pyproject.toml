[project]
name = "kyc-aml-agent"
version = "0.1.0"
description = "Agentic KYC/AML compliance system on LangGraph"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "langgraph",
    "langchain",
    "langchain-community",
    "pydantic>=2",
    "rapidfuzz",
    "lightgbm",
    "scikit-learn",
    "pandas",
    "numpy",
    "faker",
    "fastapi",
    "uvicorn",
    "streamlit",
    "python-dotenv",
    "kaggle",
    "joblib",
    "langchain-groq>=1.1.3",
]

[dependency-groups]
dev = ["pytest"]

[tool.uv]
package = false
